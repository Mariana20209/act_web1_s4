const reemplazar = (cadena) => cadena.replace(/ /g, "-"); //g significa global, es decir, que reemplace todos los espacios, hay que dejar un espacio dentro los //


console.log(reemplazar("Juan Felipe")); 